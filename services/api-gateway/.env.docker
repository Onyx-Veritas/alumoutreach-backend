# Application
NODE_ENV=development
PORT=3000

# Database
DB_HOST=postgres
DB_PORT=5432
DB_USERNAME=alumoutreach
DB_PASSWORD=alumoutreach_dev
DB_DATABASE=alumoutreach

# Redis (not used in dev compose)
REDIS_HOST=redis
REDIS_PORT=6379

# Elasticsearch (not used in dev compose)
ELASTICSEARCH_URL=http://elasticsearch:9200

# MinIO/S3 (not used in dev compose)
S3_ENDPOINT=http://minio:9000
S3_ACCESS_KEY=alumoutreach
S3_SECRET_KEY=alumoutreach_dev
S3_BUCKET=alumoutreach

# ClickHouse (not used in dev compose)
CLICKHOUSE_HOST=clickhouse
CLICKHOUSE_PORT=8123
CLICKHOUSE_DATABASE=alumoutreach

# NATS
NATS_URL=nats://nats:4222

# JWT
JWT_SECRET=dev-secret-change-in-production
JWT_EXPIRES_IN=7d

# CORS
CORS_ORIGINS=http://localhost:5173,http://frontend:4173

# Email
EMAIL_MODE=sendgrid
EMAIL_FROM=hello@alumerp.com
SENDGRID_API_KEY=
